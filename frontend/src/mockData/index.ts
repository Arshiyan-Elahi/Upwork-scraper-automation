/** Mock data barrel — used by all pages except Job Inbox (live API). */
export {
  bestPerformingNiche,
  bestPortfolioLinks,
  bestProposalStyle,
  jobsOverTime,
  pipelineMetrics,
  verdictChartData,
} from './analytics'
export { dashboardMetrics } from './dashboardMetrics'
export { jobs } from './jobs'
export { winningProposals } from './winningProposals'
